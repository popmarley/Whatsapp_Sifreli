<Window x:Class="Whatsapp_Sifreli.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Giriş"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Width="360" Height="200"
        AllowsTransparency="True"
        Background="Transparent">

    <Border CornerRadius="14" Background="#101316" Padding="18">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Uygulamayı başlatmak için parola girin"
                       Foreground="#EDEDED" FontSize="16" Margin="2,0,2,8"/>

            <PasswordBox x:Name="Pwd"
                         Height="36"
                         FontSize="14"
                         Padding="10"
                         Background="#1F2A32"
                         Foreground="#EDEDED"
                         BorderThickness="0"
                         Grid.Row="1" />

            <StackPanel Grid.Row="2" Orientation="Horizontal"
                        HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="İptal"
                        Width="90" Height="34"
                        Click="Cancel_Click"
                        Background="#2A363F"
                        Foreground="#EDEDED"
                        BorderThickness="0"
                        Cursor="Hand" Margin="0,0,8,0"/>
                <Button Content="Giriş"
                        Width="110" Height="34"
                        Click="Login_Click"
                        Background="#3BA55D"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
